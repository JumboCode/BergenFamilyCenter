import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    typography: {
        // fontFamily: '',
        h5: {
            fontFamily: 'Opens Sans',
            color: "#094d7e",
        },
        // overrides: {
        //     MuiButton: {
        //         color: 'white',
        //     },
        // }
    }
});
