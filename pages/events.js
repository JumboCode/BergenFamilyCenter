import Head from 'next/head'
import { Typography } from '@mui/material';

export default function Events() {
    return (
        <div>
            <Typography>
                Events
            </Typography>
        </div>
    )
}
